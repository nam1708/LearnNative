import React from 'react';
import { Button, Text } from 'react-native';

const Buoc = ({ navigation }) => {
    const onClick = () => {
    }
    return <Button onPress={click} title='ve home'></Button>;
};

export default Buoc;
